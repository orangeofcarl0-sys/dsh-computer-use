# HANDOFF — dsh-computer-use 交接文档（2026-09-16）

> 目的：干净工作区重新开始时，本文档 + 仓库内文档 = 完整上下文。所有结论有据可查（实验产物/测试/提交/上游 issue）。
> 本文档取代 HANDOFF-2026-09-07.md（其有效内容已全部并入）。

## 1. 项目定位与现状

- **dsh-computer-use**：DeepSeek Harness（dsh）的桌面操作插件，**个人项目**。独立仓库 `github.com/orangeofcarl0-sys/dsh-computer-use`（MIT），工作分支 **v0.3 = 默认分支 = harness 消费分支**。
- 版本 **0.5.3 系列**（tag v0.5.3 @ 12d35b8；其上有多批修正，本地提交自主，**push/tag 只在用户明说"发布"时执行**）。
- **安全立场：无感自治**——易用无感、自动提权、极危注记不阻断；凭据硬保护是唯一硬拒绝。
- 2026-09-06/07 三大件（结构密码检测修复 / S3a 视觉断言协议 / 快照新鲜度 Phase A）见历史 HANDOFF，均已真机验证。
- **2026-09-08~16 新增（本轮）**：
  1. **S4 评测回归系统落地**（evals/）：任务集 9 条（冒烟 4 + 核心 5，16 条重排退役 7 条重复项）、PS oracle 四件套（setup/oracle/cleanup/simulate）、AC2 双向独立验证、零依赖 CDP runner（Chrome headless → harness 真实会话）、`--tier/--max-steps/--pin-model/--pin-effort`、maintain/stop-stale/hygiene 运维脚本。基线 1/16 与失败模式分析已固化（PLAN-s4 §10）。
  2. **坐标契约实测钉死并修复**（本轮最重要的产品修复）：`computer_click(x,y)` 的空间 = **窗口矩形相对物理像素**（可控探针逐点标定，最小二乘残差 ≤0.55px），而 observe 元素坐标是**屏幕物理像素**——两者差一个窗口原点，是"坐标点击整体打偏"的根因。observe 输出已换算到点击空间，截图/元素/点击三者统一；同时补 `screenOrigin` + `coordinateSpace` 换算基准元数据。
  3. **dsh 0.1.5-rc.2 适配**：新版严格校验工具输出（additionalProperties:false）→ 观测降级回执未声明字段使 screen_zoom 整次失败（已修）；composer 的 `execCommand('insertText')` 失效（改用 CDP `Input.insertText`）；轮次页脚格式变更与侧栏读数污染（已修）；宿主参数表不含 additionalProperties → 未知参数静默忽略并可能回退"最前窗口"（插件层已单点拒发）。
  4. **失败语义修复**：驱动 `{"refusal":{...}}` 回执此前被当成功上报（menu_path_unavailable 仍报"已调用"）→ 抽 `lib/engine.js` 统一识别；hotkey 的 UIA 加速器超时翻译为可执行指引。
  5. **上游 issue #3908**：WinUI3 目标 hotkey 稳定失败（跨应用对照 + 复现率 + 排除本机因素，证据见 evals/tests/*.repro.mjs）。
  6. **测试面扩展**：`npm test` 六套共 **103 断言**（posture / tools / delivery-chain / schema 一致性 19 工具×22 场景 / observe 降噪 13 / computer_task 契约 16）+ 真机动作闭环（live E2E）+ verify-runtime 14/14；修掉多处测试脆弱点（Windows import URL、驱动自身窗口排除、工具数写死、断言过期）。
  7. **上下文预算 A–F**（工作区 PLAN-context-budget）：A 描述写入成本阶梯；B schema 瘦身 10176→9486 字符；C 紧凑动作回执 + `verboseReceipts`；D 树标签 100→48 与多行折首行；E `app_list` 紧凑化；**F 新增第 20 个工具 `computer_task`**（`ctx.subagents` + `toolFilter` 限 19 个桌面工具 + `outputSchema` 强制 {ok,summary,evidence} + `maxDepth=1`）。
  8. **computer_task 真机补验（同日）**：模型第 1 步即调用它（目标规范）、宿主创建子会话（`subagent/catalog`，one-shot）→ **通道端到端成立**；但子 agent 10 分钟超时（记事本保存路径受限），**父 agent 随后从头重做**（7 observe / 6 zoom / 3 click / menu / key），15 分钟预算耗尽 → 任务 timeout。**教训：委派的风险不是"子 agent 数量"而是"超时后重复劳动"**。据此加固：默认子任务超时 10→5 分钟、新增 `maxTaskCalls` 每会话委派预算（默认 3，超限拒并指向内联）、超时回执写明"先 observe 确认是否已部分完成，别从头重做"。结构上无扇出风险：`maxDepth=1` 且子 agent 的工具白名单不含 `computer_task`（子不能再生子）。

## 2. 路径与环境

| 项 | 路径/值 |
|---|---|
| 工作副本 | `F:\Codex_Work_Space\DSH plugin\dsh-computer-use`（分支 v0.3） |
| 规格文档（工作区，不入库） | `F:\Codex_Work_Space\DSH plugin\PLAN-*.md`（含 **PLAN-s4-eval-regression.md**：§3 任务集/§6 AC/§10 基线与分析/§10.6 dsh 适配与廉价通道）、`SURVEY-*.md`、`REVERSE-*.md` |
| 实验产物（不入库） | `F:\Codex_Work_Space\Public\dsv4fv-exp\`（a5/a6/a7 系列）；S4 真值报告 `evals/report-last.json` 亦不入库 |
| cua-driver | `C:\Users\66494\.cua-driver\packages\current\cua-driver.exe`（**0.23.2**；daemon 会静默退出——挂了 `serve --socket \\.\pipe\cua-driver`） |
| harness | **dsh 0.1.5-rc.2**（2026-09-14 升级；基线跑在 0.1.2-rc.1，**跨版本数字不可比**）。重启：`~/.dsh/restart-dsh-capture.ps1`（把当前 boot 的 tokened URL 落盘）；URL/token：`~/.dsh/web-latest.log` 最后一条 |
| harness 模型通道 | `~/.dsh/settings.yaml`：`agent-default-model` = `opencode-go / deepseek-v4.1-flash / high`；`llm-pi-ai.providers.opencode-go` 需 `baseURL=https://opencode.ai/zen/go/v1` + **必需请求头 `x-opencode-session`**（缺头 400 MissingSessionID）。备份 `settings.yaml.bak-pre-opencode-go-*` |
| 凭据 | `~/.dsh/.credentials.yaml`（含 `OPENCODE_GO_API_KEY`）；GLM 兜底需 `ZHIPU_API_KEY`（未配） |
| S4 沙箱 | `%USERPROFILE%\.dsh\s4-evals\<taskId>\`（任务资产；cleanup 负责清理） |
| 驱动浏览器测试 | headless Chrome + CDP（`evals/lib/cdp.mjs`，零依赖）；**发消息前校验所在会话为 0 轮**（曾漂移误发，守卫已拦）；页面侧 evaluate 是唯一可靠点击路径 |

## 3. 架构速览（详见 docs/ARCHITECTURE.md）

- **20 工具四组**（观察 observe/zoom；动作 click/double/right/type/key/scroll/drag/wait + app_list/launch；运营 verify/wait_for/clipboard/menu/hover/stop/resume；**委派 computer_task**）。
- **wrap 统一管线**（index.js）：**未知参数拒发** → 锁存闸门 → guard（凭据硬保护/极危注记/白名单）→ 实现 → 注记回填。
- **lib**：cua.js（三级投递链）、observe.js（模式决策+降级链+zoom 定位原语+结构密码标记+**坐标换算元数据**）、**engine.js（驱动拒绝回执统一识别：refusal/effect/error/code）**、ops.js、roles.js、passwordScan.js、guard.js、snapshot.js（TTL+新鲜度语义）、human.js、vision.js、attach.js。
- 底座：cua-driver daemon（薄策略层，承诺上限=驱动投影深度）。

## 4. 硬知识（实验实证，勿凭直觉推翻）

1. **坐标空间（2026-09-16 标定，取代旧"未定"结论）**：
   - `computer_click(x,y)` = **窗口矩形左上角为原点的物理像素**（= 窗口截图空间）；标定方法：自绘矩形探针 + 表单级 MouseDown 回读客户区坐标，逐点最小二乘（残差 ≤0.55px）；y 比客户区多出的量 = 标题栏（逻辑×DPI）。
   - 驱动给的元素 frame / sidecar rect = **屏幕物理像素**（A6 复核：标题栏 y=806 vs 窗口原点 y=780）。
   - **截图 = 窗口 bounds 的 1:1 物理像素裁剪**（同窗口 bounds 1906×1166 == screenshot 1906×1166）→ `screenOrigin = bounds.x/y`。
   - 插件现状：observe 输出已换算到点击空间（三者统一）；zoom 另给 `crop{scale}` + `screenOrigin`；**zoom 恒定 1.2× 上采样**（请求 200×150→返回 240×180），且**裁剪宽 ≤500px**否则拒绝。
2. **A5 证据通道**：可读 PNG 上数字转述噪声≈0（4fv 93 位零错误；**4.1f 复测 30/30 正确**）；zoom JPEG ≤500px 对 ~13px 小字不可读（诚实拒答）；**同会话历史回声风险真实**（断言必须新鲜证据）。
3. **密码框结构位**：ES_PASSWORD 在 UIA 映射 IsPassword=false（#3576）——判定须 IsPassword ∨ 样式位；标记几何=屏幕坐标直配。
4. **Win11 记事本（WinUI3）输入通道**（#3908）：`hotkey` UIA 加速器扫描 4s 超时（稳定复现 9/10）；`press_key` 静默不达（`effect:unverifiable`）；`invoke_menu` 报 `menu_path_unavailable`。对照：经典 Win32（regedit）走同一客户端路径 3/3 送达 → 目标类型特异性，非本机输入栈故障。**保存类任务需走"点控件"**（element 编号点击是可靠通道）。
5. **drv/harness 其他实测**：驱动 `verify_state` 对 **agent 自己输入进文档的文本**返回 `unknown/untrusted_source`（拒绝自证，验证要用窗口标题/外部状态）；WinUI3 应用 `Process.MainWindowHandle` 恒为 0（就绪判定用 UIA）；剪贴板可能被他进程占用（set 后立即 get 为空 → set+verify 重试）；打包应用重启竞态（杀进程后要等消失再启动）；**记事本 TabState 会复活上次会话的未保存标签**（`%LOCALAPPDATA%\Packages\Microsoft.WindowsNotepad_*\LocalState\TabState\*.bin`，清理需显式开关 `evals/hygiene.ps1 -ClearNotepadTabState`）；**记事本进程会被用户文档复用**（清理测试窗口必须按标题匹配，勿按进程名杀）。
6. **dsh 0.1.5 客户端事实**：工具输出严格校验（additionalProperties:false，未声明字段=整次失败）；composer 的 `execCommand('insertText')` 失效（用 CDP `Input.insertText`）；轮次页脚 `N 轮 M 步`（无 ·，且侧栏旧会话读数会污染判断，需取最后一次匹配）；宿主参数表编译不含 additionalProperties；新会话默认落 estest 工作区。
7. **工程坑**：PS 5.1 无 BOM 脚本按 ANSI 解析（PS1 必须 ASCII-only，中文用码点构造）；PS 大小写不敏感变量遮蔽 + 单元素数组展开；`npm run check` 曾因 glob 失效（已改 tests/syntax.check.mjs）；Windows 上动态 import 必须 file:// URL；Bash heredoc 会吞 `\\`（正则/路径转义要小心）；驱动 daemon 与 harness 偶发挂死（重启即过，瞬态）。

## 5. 测试与验证体系

- **`npm test`（改码必跑）**：posture.smoke（安全姿态+凭据+快照新鲜度 I 系列）/ tools.smoke / delivery-chain.smoke / **schema.conformance（19 工具 × 22 场景，按声明 schema 严格校验所有返回形态——dsh 0.1.5 严格校验类问题的守门测试）**；另 `npm run check`（语法）+ `verify-runtime.mjs`（真驱动 12/12）+ `tests/live-action.e2e.mjs`（真桌面动作闭环，默认 3 轮）。
- **evals/**：`verify-oracles.mjs`（AC2：setup 必须 ok → oracle 空跑必失败 → simulate 后必通过 → cleanup 干净）；`run.mjs`（S4 runner）；诊断脚本 `tests/click-space*.diag.mjs`（坐标标定）、`tests/key-channel.diag.mjs`、`tests/uia-hotkey.repro.mjs`、`tests/key-via-plugin.diag.mjs`。
- 重构零行为变化用 parity/JSON 比对法；行为变更如实进 CHANGELOG。

## 6. 文档地图

| 文档 | 位置 | 内容 |
|---|---|---|
| ROADMAP.md | 仓库 | A 事实澄清（全部闭环）/ C 驱动库存候补 / D 驱动缺口 / E 远期 |
| docs/ARCHITECTURE.md | 仓库 | 4 图 + 模块表（含 engine.js 后需补） |
| PERMISSIONS.md | 仓库 | 权限面 + 承诺验证矩阵 |
| VERIFICATION.md | 仓库 | macOS 原始验证 + Windows 运行时验证章节 |
| CHANGELOG.md | 仓库 | 0.4.0~0.5.3 全清单（含 09-16 批：快照/坐标/降噪/上下文预算 A–F） |
| PLAN-context-budget.md | 工作区 | 上下文预算 spec（A–E 逐项 + **F subagent 化**推演与实现记录） |
| **evals/README.md** | 仓库 | S4 测试系统：任务集与优化原则、环境知识（实测坑）、模型通道配方、运行方式 |
| PLAN-s4-eval-regression.md | 工作区 | S4 spec + §10 基线与分析 + §10.6 dsh 适配与廉价通道 |
| PLAN-snapshot-freshness.md / PLAN-s3-model-verifier.md | 工作区 | 领域 spec（Phase B/C 待上游、S3b 待观察者条件） |
| REVERSE-zcode-trae-computer-use.md | 工作区 | 竞品逆向与三点落地路线 |

## 7. 排期与待办

**A. 事实澄清**：全部闭环或定性。遗留 A2 GLM 漂移 / A3 qwen 帧错位（需本地代理稳定）。

**B. 功能/改进候选（按价值排序）**：
1. **驱动 daemon 提权问题**：driver spawn 的窗口非提权侧杀不掉（UIPI），残留窗口会污染后续任务观察面（基线 t07 级联失败的元凶之一）。先以非提权重启 daemon 验证，再看是否需上游。
2. **observe 降噪**：observe:动作 ≈ 2-3:1 是 S4 步速的主因（单回合 input tok 曾达 8.5M）；增量/去重与快照语义天然衔接。
3. **S4 任务级验证**：harness UI 空闲时跑 `--tier smoke --max-steps 40 --timeout-min 8`（当前新会话创建受 UI 状态影响，需先确认能切新会话）。
4. 快照新鲜度 Phase B/C（等 #3586/#3587）；S3b（需 ZHIPU_API_KEY）；P5 驱动级候补（hover 语义/middle_click/多显示器）。

**C. 上游跟踪**：#3576（密码投影）、#3586（树版本/条件请求）、#3587（干预信号）、**#3908（WinUI3 hotkey UIA 加速器超时，本轮提交）**、坐标点击缺陷（A7 素材，随 #3908 一并说明）、daemon 崩溃观察。

**D. 立即可做**：无阻塞项。下次"发布"= push 全部本地未推提交（快照新鲜度 + S4 系统 + 本轮修复）。

## 8. 工作约定（用户偏好，务必遵守）

1. **spec 先行**：详细推演 → 按 spec 规范落文档（背景/目标/规格/AC/风险/分歧/审计）→ 分歧点 AskUserQuestion（简洁直接符合直觉，默认不打折全面方案优先）→ 拍板后逐阶段实现，每阶段审计测试。
2. **结构直接简洁，严禁兼容层**；重构零行为变化需机械验证。
3. **测量驱动**：方案先实验实证，被数据否决的果断放弃（SoM/双原点/坐标语义/A7 三假设 四案例）。
4. **权限面诚实**：PERMISSIONS/summary/矩阵与实现一致。
5. **发布 gated**：push/tag/消费切换只在用户明说"发布"时执行；本地提交自主。
6. 文档中文；提交信息中文一句话（版本+主题）；LF/CRLF 警告忽略。
7. 真值/实验产物不进仓库；仓库只进脚本与文档。
8. **测试窗口用完即清**（用户明确要求）：探针自带超时自关 + 脚本末尾显式清理 + 复核残留；清理按**标题**匹配，避免误伤用户窗口（Win11 记事本单进程多标签，进程可能同时挂着用户文档——绝不按进程名杀）。
9. **禁止在 bash 命令里用反引号**（含 `node -e "..."` 内联脚本）——反引号先被 shell 展开，会静默吞掉 Markdown/代码片段并可能执行意外命令（2026-09-16 连续踩三次）。写文件/改文档一律用编辑器工具；必须内联时用 heredoc + 引号包裹且内容里不放反引号。

## 9. 下一步建议

1. 修 daemon 提权/清理问题（环境噪声源，影响所有后续测试可信度）。
2. 做 observe 降噪（既是产品优化也是 S4 提速的最大杠杆）。
3. harness 空闲时跑冒烟组，验证 #3908 的实战影响与"路径受阻时换控件"的自我恢复能力。
4. 用户说"发布"时 push 全部本地提交。
