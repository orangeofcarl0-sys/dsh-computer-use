# HANDOFF — dsh-computer-use 交接文档（2026-09-07）

> 目的：干净工作区重新开始时，本文档 + 仓库内文档 = 完整上下文。所有结论有据可查（实验产物/测试/提交/上游 issue）。
> 本文档取代 HANDOFF-2026-09-06.md（其有效内容已全部并入）。

## 1. 项目定位与现状

- **dsh-computer-use**：DeepSeek Harness（dsh）的桌面操作插件，**个人项目**。独立仓库 `github.com/orangeofcarl0-sys/dsh-computer-use`（MIT，历史重写为 orangeofcarl0 唯一贡献者），工作分支 **v0.3 = 默认分支 = harness 消费分支**（profile 依赖 git+https 无 pin，跟随 HEAD）。
- 当前版本 **0.5.3 系列**（tag v0.5.3 @ 12d35b8；其上已有修正/追加批次，最近本地提交见 git log）。**发布约定：push/tag 只在用户明说"发布"时执行，本地提交自主。**
- **安全立场：无感自治**（0.4.0 拍板）——易用无感、自动提权、极危注记不阻断；凭据硬保护是唯一硬拒绝。
- 2026-09-06/07 两日新增三大件（均已真机验证）：
  1. **结构密码检测修复**：A6 E2E 发现 0.5.3 标记几何按"窗口本地+原点换算"旧假设实现，实测驱动 frame 与 sidecar rect 同为屏幕物理像素——已改屏幕坐标直配（a1b1069），guard 结构位硬拒真正生效。
  2. **S3a 视觉断言协议**（2137960）：A5 实证（可读 PNG 上 dsv4fv 数字转述噪声≈0、zoom JPEG 小字不可读、诚实拒答 2/2）落为 observe/zoom/verify 三工具描述协议。
  3. **快照新鲜度语义 Phase A**（f5f59e5）：失效原因三分类（[snapshot_expired]/[snapshot_mismatch]/[snapshot_consumed]）+ supersession off/note/enforce 默认 note + 七动作工具显式 snapshot_id 参数 + unverifiable 状态可疑注记。
- 本机消费：`~/.dsh/profiles/web/package.json` → git+https 分支 HEAD；改动后需 pnpm update（或文件同步）+ harness 重启才生效。**重启偶发挂死**（进程满核热循环、HTTP 无响应）——重跑 restart-dsh.ps1 即过，瞬态非代码问题（2026-09-07 定性）。

## 2. 路径与环境

| 项 | 路径/值 |
|---|---|
| 工作副本 | `F:\Codex_Work_Space\DSH plugin\dsh-computer-use`（分支 v0.3） |
| 规格文档（工作区，不入库） | `F:\Codex_Work_Space\DSH plugin\PLAN-*.md`（usability-first / tools-v0.5 / credential-guard / s3-model-verifier / snapshot-freshness）、`SURVEY-computer-use-landscape.md`、`REVERSE-zcode-trae-computer-use.md`（§6 三点详细展开） |
| 实验产物 | `F:\Codex_Work_Space\Public\dsv4fv-exp\`（a6/a7/a5 系列 E2E 脚本与记录） |
| cua-driver | `C:\Users\66494\.cua-driver\packages\current\cua-driver.exe`（0.23.2；daemon 会静默退出——挂了就 `serve --socket \\.\pipe\cua-driver` 重启） |
| harness | 重启 `~/.dsh/restart-dsh.ps1`（脚本超时判断保守，慢启动等 1-2 分钟即可）；URL token：`~/.dsh/get-dsh-url.ps1`；浏览器勿用 Edge |
| 凭据 | `~/.dsh/.credentials.yaml`；GLM 兜底需 `ZHIPU_API_KEY`（未配） |
| harness 驱动浏览器测试 | iab 标签易崩（about:blank/空白体）——重建标签→token 或裸 URL→**页面侧 evaluate 点击**切会话（sessionRow class），**以统计脚注（N 轮）校验所在会话**，防误发 |

## 3. 架构速览（详见 docs/ARCHITECTURE.md）

- **19 工具三组**（观察组 observe/zoom；动作组 click/double/right/type/key/scroll/drag/wait + app_list/launch；运营组 verify/wait_for/clipboard/menu/hover/stop/resume）。
- **wrap 统一管线**：锁存闸门 → guard（凭据硬保护/极危注记/白名单）→ 实现 → 注记回填。
- **lib**：cua.js（三级投递链）、observe.js（模式决策+降级链+zoom 定位原语+结构密码标记）、ops.js、roles.js、passwordScan.js、guard.js、**snapshot.js（TTL + 新鲜度语义：三分类失效原因/supersession 门禁/消费与可疑标记）**、human.js、vision.js、attach.js。
- 底座：cua-driver daemon（薄策略层，承诺上限=驱动投影深度）。

## 4. 硬知识（实验实证，勿凭直觉推翻）

1. **坐标系（A6/A7 实证，2026-09-06）**：驱动 frame 与 sidecar rect 同为**屏幕锚定物理像素**（逐像素相等；sidecar 经 node 派生继承 DPI 感知，bash 直跑得逻辑 px；本机 DPI=150%）；截图为窗口裁剪（606×287）而 frame 为屏幕 px。**坐标像素点击语义未定**——驱动 describe 契约=window-local PNG px 但实测与三种假设均有反例（DPI 混合迹象），插件层不可判定维持现状（windowLocalOf 恒等），element 模式（token）为唯一已证可靠路径。
2. **A5 证据通道决定论**：可读 PNG 证据上 dsv4fv 数字转述噪声≈0（93 位零错误）；zoom JPEG ≤500px 对 ~13px 小字不可读（诚实拒答 2/2 无编造）；**同会话历史回声风险真实**（读图断言必须新鲜证据）。淡字形混淆对：6↔9、7↔1、8↔3、1↔2、5↔6。
3. **密码框结构位**：ES_PASSWORD 样式位在 UIA 映射 IsPassword=false（#3576）——判定必须 IsPassword ∨ 样式位双信号；标记几何=屏幕坐标直配（旧双原点换算会标错相邻元素）。
4. **dsv4fv vision 观察者只出思考不返回文本**（A1 实测）——观察者模式半通；GLM 兜底需 ZHIPU_API_KEY。
5. **Windows 前台锁三级链**、XAML/WinUI 组合键走 hotkey、角色命名按平台原生——见 docs/ARCHITECTURE.md 与 PLAN-usability-first。
6. **工程坑**：PS 5.1 无 BOM 脚本按 ANSI 解析（探针必须 ASCII-only）；PS 大小写不敏感变量遮蔽 + 单元素数组展开会静默改语义（探针踩过）；ReadOnly 框 ValuePattern 写入驱动假报成功；`npm run check` 在 Windows 不展开 glob（逐文件 node --check）；驱动 daemon 与 harness zoom 调用两次时间相关崩溃（未证实因果，观察中）。
7. **harness 浏览器自动化**：IAB 标签偶发崩溃丢页面；Playwright 按钮 actionability 常超时——统一用页面侧 evaluate 点击（find aria-label）；发送消息只用"发送消息"按钮（Enter 不发送）；**发消息前必须校验所在会话**（曾误发用户旧会话，已即时止损）。

## 5. 测试与验证体系

- 三套离线 smoke（stub 驱动）：`tests/posture.smoke.mjs`（34：安全姿态+凭据 G/H+快照新鲜度 I 系列）、`tests/tools.smoke.mjs`（15）、`tests/delivery-chain.smoke.mjs`（3）——**52 项全绿是基线**，改码必跑。
- 运行时脚本：`verify-runtime.mjs`（隔离加载，注意其"12-13 工具"断言已过时）；实验 E2E 在 `dsv4fv-exp/a5|a6|a7-*.mjs`。
- VERIFICATION.md 已追加 Windows 运行时验证章节（A6 9/9、新鲜度真机 4/4、A1 通道、A7、A5）。
- 重构零行为变化用 parity/JSON 比对法；行为变更如实进 CHANGELOG。

## 6. 文档地图

| 文档 | 位置 | 内容 |
|---|---|---|
| ROADMAP.md | 仓库 | A 事实澄清（A1/A4/A6/A7 已闭环留档） / C 驱动库存候补 / D 驱动缺口（坐标缺陷 + #3586/#3587 链接）/ E 远期（S3a 已落地） |
| docs/ARCHITECTURE.md | 仓库 | 4 图 + 模块表（snapshot 新鲜度已更新） |
| PERMISSIONS.md | 仓库 | 权限面 + 承诺验证矩阵（无新权限面：supersession/snapshot_id 零 spawn 零网络） |
| VERIFICATION.md | 仓库 | macOS 原始验证 + Windows 运行时验证追加章节 |
| CHANGELOG.md | 仓库 | 0.4.0~0.5.3 全清单（含本批 Fixed/Added） |
| PLAN-s3-model-verifier.md | 工作区 | S3 spec（A5 数据表 + S3a/S3b 分期） |
| PLAN-snapshot-freshness.md | 工作区 | 快照新鲜度 spec（推演/规格/AC/风险/拍板记录） |
| REVERSE-zcode-trae-computer-use.md | 工作区 | ZCode/Trae 逆向（§6 三点详细展开：revision/supersession/干预三件套 → dsh 落地路线） |

## 7. 排期与待办

**A. 事实澄清**：全部闭环或定性——A1 ✓（native 通/vision 观察者半通）、A4 ✓、A6 ✓（顺带修坐标 bug）、A7 ✓（驱动缺陷定性）、A5 ✓（S3 输入）。唯一遗留：**A2 GLM 漂移复测**（需本地代理稳定）、**A3 qwen 帧错位**（同前）。

**B. 功能/改进候选**：
1. **快照新鲜度 Phase B/C**：等上游 #3586/#3587 动向——落地后 TTL 退役为上限保险、supersession 可升 enforce、suspect 由真实干预驱动。
2. **S3b**：screen_assert 工具 / 独立观察者裁决（前置：ZHIPU_API_KEY 或 dsv4fv 观察者文本修复）。
3. **S4 评测回归**：10-20 条 WAA 式 PowerShell oracle 任务集。
4. **P5**：hover 真实语义、middle_click/多显示器（驱动级）。
5. **驱动库存候补**（ROADMAP C）：browser_*、set_window_frame、trajectory、kill_app。

**C. 上游跟踪**：#3576（密码投影）、#3586（树版本/条件请求）、#3587（干预信号）、坐标点击缺陷（素材备于 dsv4fv-exp/a7-*，暂不提交）、daemon 崩溃观察。

**D. 立即可做**：无阻塞项。下次"发布"= push 本地 2 个未推提交（f5f59e5 快照新鲜度 + 89eb16b 文档；其后如有新增一并推）。

## 8. 工作约定（用户偏好，务必遵守）

1. **spec 先行**：详细推演 → 按 spec 规范落文档（背景/目标/规格/AC/风险/分歧/审计）→ 分歧点 AskUserQuestion（简洁直接符合直觉，默认不打折全面方案优先）→ 拍板后逐阶段实现，每阶段审计测试。
2. **结构直接简洁，严禁兼容层**；重构零行为变化需机械验证（parity/JSON 比对）。
3. **测量驱动**：方案先实验实证，被数据否决的果断放弃（SoM/双原点/坐标语义三次案例）。
4. **权限面诚实**：PERMISSIONS/summary/矩阵与实现一致。
5. **发布 gated**：push/tag/消费切换只在用户明说"发布"时执行；本地提交自主。
6. 文档中文；提交信息中文一句话（版本+主题）；LF/CRLF 警告忽略。
7. 真值/实验产物不进仓库（`dsv4fv-exp/`），仓库只进脚本与文档。

## 9. 下一步建议

1. 用户说"发布"时：push + （可选）版本里程碑判断（当前积为 0.5.3 修正批）。
2. 跟踪 #3586/#3587 上游响应；有动作即评估 Phase B/C 启动。
3. 配 ZHIPU_API_KEY 解锁 vision 兜底与 S3b 前置。
4. S4 评测回归任务集（下个自然大项）。
